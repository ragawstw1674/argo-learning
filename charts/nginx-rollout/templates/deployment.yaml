# apiVersion: apps/v1
# kind: Deployment
# metadata:
#   name: nginx-deployment
# spec:
#   replicas: 1
#   selector:
#     matchLabels:
#       app: nginx-dep
#   template:
#     metadata:
#       labels:
#         app: nginx-dep
#     spec:
#       containers:
#         - name: nginx
#           image: nginx:latest
#           ports:
#             - containerPort: 80
#           env:
#             - name: API_KEY
#               valueFrom:
#                 secretKeyRef:
#                   name: nginx-secret
#                   key: api-key
#           volumeMounts:
#             - name: config-volume
#               mountPath: /usr/share/nginx/html/index.html
#               subPath: message
#       volumes:
#         - name: config-volume
#           configMap:
#             name: nginx-config
